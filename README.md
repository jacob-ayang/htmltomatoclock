# 番茄钟（静态 HTML + JS）

快速的静态实现，可直接在浏览器中打开 `index.html` 使用。

功能
- 工作 / 短休息 / 长休息 切换
- 开始 / 暂停 / 重置
- 可配置工作时长、短休、长休与每轮次数
- 快捷键：空格（开始/暂停），R（重置）

使用
1. 在文件管理器中打开 `index.html`，或在本地使用静态服务器（如 `npx http-server`）
2. 调整时间设置，点击“开始”即可

注意
- 声音使用浏览器 WebAudio，某些浏览器需要用户交互后才能播放声音
